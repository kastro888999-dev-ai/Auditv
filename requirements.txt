# Video-to-Markdown pipeline requirements
# Install inside the project venv:
#   ./venv/bin/pip install -r requirements.txt

openai-whisper>=20250625
torch>=2.6
yt-dlp>=2025.1.1
gradio>=6
psutil>=5.9   # RAM/CPU por proceso para el panel de equipo de la GUI
